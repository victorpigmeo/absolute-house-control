/**
 * Domain layer for the greenhouse service, split into {@code models} (commands and domain objects),
 * {@code entity} (JPA entities), {@code exception} (business exceptions), and {@code service}
 * (domain services) subpackages.
 */
package dev.pigmeo.ahc.greenhouse.domain;
